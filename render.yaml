services:
  - type: web
    name: youtube-downloader-fullstack
    env: docker
    plan: free
    # Context is now root because Dockerfile needs access to both backend and frontend folders
    dockerContext: .
    dockerfilePath: Dockerfile
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000